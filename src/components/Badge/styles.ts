import styled from "styled-components";

const StyledBadge = styled.span`
  padding: 4px 13px;
  box-sizing: border-box;
  font-weight: 400;
  font-size: 12px;
  color: #ffff;
  background: #23c9ff;
  border-radius: 10px;
`;

export { StyledBadge };
